name: flutter_ffi_jscore
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: '>=2.7.0 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
    
  ffi: ^0.1.3
  http: ^0.12.2
  sync_http: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/js/